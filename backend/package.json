{
  "name": "ems-backend",
  "version": "1.0.0",
  "private": true,
  "description": "JSON-Server backend for EMS",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 3001",
     "sync": "node sync-db.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}


